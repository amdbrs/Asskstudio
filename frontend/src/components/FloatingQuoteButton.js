import { Link } from 'react-router-dom';
import { FileText } from 'lucide-react';

export const FloatingQuoteButton = () => {
  // Disabled - no floating button
  return null;
};

export default FloatingQuoteButton;
